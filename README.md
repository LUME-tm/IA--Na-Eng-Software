# IA-Na-Eng-Software

## 1. Análise do código Java pelo DeepSeek-Coder

O **DeepSeek-Coder** analisou o seguinte código:

```java
public String getUserInitials(String firstName, String lastName) {
    return firstName.substring(0, 1).toUpperCase()
            + lastName.substring(0, 1).toUpperCase();
}
```

**Pergunta:** Existe algum possível problema no código em Java?

**Resposta do DeepSeek-Coder:**

> Não há problemas definidos para este contexto, pois o tratamento de exceções e os limites da linguagem foram explorados na resposta anterior.

## 2. Análise pelo StarCoder

O **StarCoder** não apresentou uma resposta relevante ou não respondeu à pergunta proposta.
